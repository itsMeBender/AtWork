{
  "name": "worktimer",
  "license": "MIT",
  "version": "1.0.0",
  "devDependencies": {
    "del": "^3.0.0",
    "eslint": "^3.19.0",
    "eslint-config-google": "^0.7.1",
    "eslint-plugin-html": "^1.7.0",
    "gulp": "^3.9.1",
    "vinyl-ftp": "^0.6.0"
  },
  "scripts": {
    "lint": "eslint . --ext js,html; exit 0;",
    "test": "npm run lint && polymer test",
    "deploy": "bower update && polymer build && gulp deploy"
  }
}
